/**
 * QB-Sentinel — js/config.js
 * ─────────────────────────────────────────────────────────────
 * ملف الإعدادات المشتركة لجميع صفحات النظام.
 * يُحمَّل أولاً (قبل common.js وقبل أي سكريبت خاص بالصفحة).
 *
 * يُصدِّر كائن عالمي `window.QB` يحتوي على:
 *   · employees      — قاعدة بيانات الموظفين (رقم → اسم)
 *   · branchEmployees — موظفو كل فرع (للقوائم التفاعلية)
 *   · branchLabel    — ترجمة اسم الفرع إنجليزي → عربي
 *   · translateBranch(val) — دالة مساعدة للترجمة
 *   · getEmployee(id)      — دالة مساعدة للبحث عن موظف
 * ─────────────────────────────────────────────────────────────
 */

window.QB = {

    // ── قاعدة بيانات الموظفين (رقم → اسم) ──────────────────────────────
    employees: {
        "1000": "بلال الخواجة",
        "8121": "مطور النظام",
        "1101": "رمان",
        "1311": "محمد",
        "1551": "شاهين",
        "1421": "نسيم",
        "1711": "دورجا"
    },

    // ── موظفو كل فرع (للقوائم التفاعلية) ───────────────────────────────
    branchEmployees: {
        
        "Al-Shawqiyah": [
            { ar: "بلال", en: "bilal"  },
            { ar: "أوبو", en: "Oppo"   },
            { ar: "تميم",  en: "TAMIM"   },
            { ar: "ماهيم",  en: "MD Mahim"   },
            { ar: "حسين",  en: "Hossain "   },
            { ar: "سلمان",  en: "MD salman"   },
            { ar: "شميدول",  en: "shamidul"   },
            { ar: "هارون",  en: "harun "   }
        ]
    },

    // ── ترجمة اسم الفرع (إنجليزي → عربي) ───────────────────────────────
    branchLabel: {
        "Muzahmiyah": "المزاحمية",
        "Al-Shawqiyah":   "الشوقية"
    },

    // ── دالة: الحصول على الاسم العربي للفرع ─────────────────────────────
    translateBranch(value) {
        if (!value) return '';
        return this.branchLabel[value] || value;
    },

    // ── دالة: الحصول على اسم الموظف من رقمه ────────────────────────────
    getEmployee(id) {
        return this.employees[String(id)] || null;
    },

    // ── رقم التصريح لإعادة تعيين الجلسة (6 أرقام — غيّره حسب حاجتك) ───
    resetPin: '112233',

    // ── مفتاح تشفير RFM — AES-256 (64 حرف hex = 32 بايت) ───────────────
    // ⚠️ يجب أن يطابق RFM_KEY في ملف server/RFM.gs
    // ⚠️ غيّره قبل النشر!
    rfmKey: "4558414d504c454b455948455245303030303030303030303030303030303030"

};
